<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="downloadmodel.list">
      <field name="name">downloadmodel list</field>
      <field name="model">downloadmodel.downloadmodel</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="downloadmodel.action_window">
      <field name="name">downloadmodel window</field>
      <field name="res_model">downloadmodel.downloadmodel</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="downloadmodel.action_server">
      <field name="name">downloadmodel server</field>
      <field name="model_id" ref="model_downloadmodel_downloadmodel"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="downloadmodel" id="downloadmodel.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="downloadmodel.menu_1" parent="downloadmodel.menu_root"/>
    <menuitem name="Menu 2" id="downloadmodel.menu_2" parent="downloadmodel.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="downloadmodel.menu_1_list" parent="downloadmodel.menu_1"
              action="downloadmodel.action_window"/>
    <menuitem name="Server to list" id="downloadmodel" parent="downloadmodel.menu_2"
              action="downloadmodel.action_server"/>
    -->
  </data>
</odoo>